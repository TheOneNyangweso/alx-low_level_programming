#include <unistd.h>
#include "main.h"

/**
 * add- function adds two integers and outputs result
 * @b: parameter for accepting first int.
 * @c: parameter for accepting second int.
 * Return: sum of b and c.
 */

int add(int b, int c)
{
	int result;

	result = b + c;
	return (result);
}
